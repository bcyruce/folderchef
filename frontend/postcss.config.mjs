/**
 * FolderChef — PostCSS Configuration
 * =====================================
 *
 * PostCSS is a tool that processes CSS. We use it here to:
 *   1. Enable Tailwind CSS (our styling framework)
 *
 * You usually don't need to edit this file.
 */

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
